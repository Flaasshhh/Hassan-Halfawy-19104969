<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Form</title>
    <link href="https://static.vecteezy.com/system/resources/previews/002/406/629/non_2x/app-development-at-office-concept-illustration-of-people-characters-in-flat-design-vector.jpg" rel="stylesheet">
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="navbar-nav">
            <a class="nav-item nav-link" href="/dashboard">Dashboard</a>
            <a class="nav-item nav-link" href="/login-history">Login History</a>
            <button id="log-out" class="btn btn-danger ml-auto">Log Out</button>
        </div>
    </nav>
    <div class="login-container">
        <form id="login-form">
            <input type="text" id="username" placeholder="Username" required>
            <input type="password" id="password" placeholder="Password" required>
            <button type="submit">Login Page </button>
        </form>
    </div>
<style>
    body, html {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
    font-family: Arial, sans-serif;
    background-color: #f5f5f5; /* Background color for the body */
}

.login-container {
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    width: 80%; /* Adjust the width as needed */
    max-width: 300px; /* Set a max-width for responsiveness */
    margin: 20px auto; /* Center the container */
    padding: 20px;
    border: 1px solid #ccc;
    background-color: #fff; /* Background color for the container */
}

#login-form input,
#login-form button {
    width: 100%;
    margin-bottom: 10px;
    padding: 10px;
    box-sizing: border-box;
}

#login-form button {
    background-color: #007bff;
    color: #fff;
    border: none;
    cursor: pointer;
}

#login-form button:hover {
    background-color: #0056b3; /* Adjust the hover color */
}

</style>
    

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
  <script>
    $(document).ready(function(){


        $('#login-form').on('submit', function(e) {
        e.preventDefault();
        var username = $('#username').val();
        var password = $('#password').val();

        $.ajax({
            url: '/api/login',
            type: 'POST',
            contentType: 'application/json',
            data: JSON.stringify({ username: username, password: password }),
            success: function(response) {
                if (response === true) {
                    window.location.href = '/dashboard';
                } else {
                    alert('Wrong Credentials');
                }
            },
            error: function() {
                alert('An error occurred. Please try again later.');
            }
        });
    });


    });
  </script>

</body>
</html>
